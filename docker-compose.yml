version: "2"

services:

  web:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - .:/code
      - ./docker/config/site.conf:/etc/nginx/conf.d/default.conf

  php:
    image: php:7.2-fpm
    user: "${UID}:${GID}"
    volumes:
      - .:/code
